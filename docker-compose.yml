version: '3.8'

services:
  app:
    image: node:latest
    container_name: musikverein-muenster
    restart: unless-stopped
    working_dir: /usr/src/app
    ports:
      - "8790:3000"
    environment:
      - PORT=3000
      # PocketBase configuration
      - POKETBASE=${POKETBASE}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      # MinIO configuration
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_PORT=${MINIO_PORT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
    volumes:
      - .:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    command: sh -c "git config --global --add safe.directory /usr/src/app && git config --global --add safe.directory /usr/src/app/src/components/databaseMusiconn && npm install && npm run build && NODE_ENV=production npm start"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

volumes:
  node_modules:
